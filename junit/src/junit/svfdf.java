package junit;

import org.junit.Assert;

public class svfdf {

}
void testMultiply() {
	int result= math.multiply(-5,-1);
	Assert.assertEquals(5, result);
}
void testMultiply() {
	int result= math.multiply(-5,-1);
	Assert.assertEquals(5, result);
}
